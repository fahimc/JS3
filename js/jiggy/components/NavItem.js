(function(window) {
	extend(NavItem,UIElement);
function NavItem()
{
	var public =Footer.prototype;
	//public.element =document.createElement('div');
	//public.element.style.position = "absolute";
	
}
window.NavItem = NavItem;
}(window));